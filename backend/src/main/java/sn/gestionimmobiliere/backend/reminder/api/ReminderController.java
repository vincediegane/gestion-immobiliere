package sn.gestionimmobiliere.backend.reminder.api;import java.util.UUID;import org.springframework.web.bind.annotation.*;import io.swagger.v3.oas.annotations.tags.Tag;import sn.gestionimmobiliere.backend.reminder.application.ReminderService;@RestController @Tag(name="Reminders") public class ReminderController{private final ReminderService service;public ReminderController(ReminderService s){service=s;}@PostMapping("/api/rent-charges/{id}/reminder-preview")public ReminderResponse prepare(@PathVariable UUID id){return service.prepare(id);}}

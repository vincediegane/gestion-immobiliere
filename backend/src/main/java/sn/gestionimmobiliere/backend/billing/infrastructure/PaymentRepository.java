package sn.gestionimmobiliere.backend.billing.infrastructure;import java.util.*;import org.springframework.data.jpa.repository.JpaRepository;import sn.gestionimmobiliere.backend.billing.domain.Payment;public interface PaymentRepository extends JpaRepository<Payment,UUID>{List<Payment> findAllByRentChargeIdAndOrganizationIdOrderByPaymentDateDesc(UUID chargeId,UUID organizationId);Optional<Payment> findByOrganizationIdAndIdempotencyKey(UUID organizationId,String idempotencyKey);}

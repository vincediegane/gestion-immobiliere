package sn.gestionimmobiliere.backend.identity.infrastructure;import java.util.*;import org.springframework.data.jpa.repository.JpaRepository;import sn.gestionimmobiliere.backend.identity.domain.AppUser;public interface AppUserRepository extends JpaRepository<AppUser,UUID>{Optional<AppUser> findByEmailIgnoreCaseAndStatus(String email,String status);boolean existsByEmailIgnoreCase(String email);}

package sn.gestionimmobiliere.backend.receipt.infrastructure;import java.util.*;import org.springframework.data.jpa.repository.JpaRepository;import sn.gestionimmobiliere.backend.receipt.domain.Receipt;public interface ReceiptRepository extends JpaRepository<Receipt,UUID>{Optional<Receipt> findByRentChargeIdAndOrganizationId(UUID chargeId,UUID org);Optional<Receipt> findByIdAndOrganizationId(UUID id,UUID org);}
